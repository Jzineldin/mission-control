{
  "name": "@openclaw/mission-control",
  "version": "1.0.0",
  "description": "macOS-native dashboard for OpenClaw AI agents",
  "main": "server.js",
  "author": "OpenClaw Community",
  "license": "BSL-1.1",
  "keywords": ["openclaw", "dashboard", "ai", "agent", "mission-control"],
  "scripts": {
    "start": "node scripts/ensure-build.js && node server.js",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install",
    "setup": "node scripts/auto-setup.js"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": "",
  "dependencies": {
    "express": "^4.22.1",
    "multer": "^2.0.2"
  }
}